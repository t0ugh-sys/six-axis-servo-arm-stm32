# 串口控制协议（可选，建议先用最简单的）

本仓库的 `firmware/Src/arm_cmd.c` 提供了一个“够用”的行文本协议示例：

## 一行命令（ASCII）

- 设置关节角（单位：度）：
  - `J0=90 J1=45 J2=120 T=1000`
  - `T`：运动时长，单位 ms（可省略，默认 500ms）

- 直接设置某通道脉宽（单位：us，用于标定）：
  - `U0=1500`
  - `U3=1200`

说明：
- 关节编号按 `arm_config.h` 的通道映射。
- 建议上位机每次发完以 `\\r\\n` 结尾。

